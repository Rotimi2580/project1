#include "main.h"

/**
 * _abs - Prints absolute values of numbers.
 * @n: n is an integer.
 * Return: Integer.
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
